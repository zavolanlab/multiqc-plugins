name: Build MultiQC

on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-10.15, ubuntu-20.04]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: echo-test
        run: echo 123
